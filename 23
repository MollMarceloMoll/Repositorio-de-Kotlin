fun main() {
    var matriz = arrayOf (
    	intArrayOf(1,2,3),
        intArrayOf(4,5,6),
        intArrayOf(7,8,9)
        )
        
        for (i in (0 until 3)){
        	println()
            for (j in (0 until 3)){
            	print("${matriz[i][j]},")
            }
        }
}
